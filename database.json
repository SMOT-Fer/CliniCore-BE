{
  "dev": {
    "driver": "pg",
    "user": "${DB_USER}",
    "password": "${DB_PASSWORD}",
    "host": "${DB_HOST}",
    "port": "${DB_PORT}",
    "database": "${DB_NAME}",
    "schema": "public"
  },
  "production": {
    "driver": "pg",
    "user": "${DB_USER}",
    "password": "${DB_PASSWORD}",
    "host": "${DB_HOST}",
    "port": "${DB_PORT}",
    "database": "${DB_NAME}",
    "schema": "public",
    "ssl": true
  }
}
